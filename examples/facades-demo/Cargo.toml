[package]
name = "facades-demo"
license = "MIT OR Apache-2.0"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# Use the consolidated rf crate with prelude
rf = { path = "../../crates/rf" }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
