[package]
name = "rf-cache-facade"
version = "0.1.0"
edition = "2021"
description = "Laravel-style Cache facade for RustForge (re-export of rf-cache)"
license = "MIT OR Apache-2.0"

[dependencies]
rf-cache = { path = "../rf-cache" }
