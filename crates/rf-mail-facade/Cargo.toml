[package]
name = "rf-mail-facade"
version = "0.1.0"
edition = "2021"
description = "Laravel-style Mail facade for RustForge"
license = "MIT OR Apache-2.0"

[dependencies]
rf-mail = { path = "../rf-mail" }
once_cell = "1.19"
tokio = { version = "1.35", features = ["sync"] }

[dev-dependencies]
tokio = { version = "1.35", features = ["sync", "macros", "rt"] }
