[package]
name = "rf-oauth"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
reqwest = { workspace = true }
# oauth2 = "4.4"  # Removed: not used in source; rf-oauth implements OAuth flows
# directly via reqwest. Re-add with oauth2 = "5.0" (reqwest 0.12 compatible)
# if we want the typed OAuth2 client builder in the future.
url = "2.5"
rand = "0.8"
tokio = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[package.metadata.rustforge]
tier = "beta"
