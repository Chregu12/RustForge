[package]
name = "rf-scheduling"
description = "Re-export of rf-scheduler for backwards compatibility"
edition.workspace = true
version.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
rf-scheduler = { path = "../rf-scheduler" }
