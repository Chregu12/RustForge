[package]
name = "rf-storage-facade"
version = "0.1.0"
edition = "2021"
description = "Laravel-style Storage facade for RustForge (re-export of rf-storage)"
license = "MIT OR Apache-2.0"

[dependencies]
rf-storage = { path = "../rf-storage" }
