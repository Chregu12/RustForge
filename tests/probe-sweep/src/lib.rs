// Probe-sweep integration-test crate.
//
// All load-bearing tests live under tests/*.rs (integration tests).
// This lib is intentionally empty — it exists only so that Cargo
// recognises the crate as having a library target.
